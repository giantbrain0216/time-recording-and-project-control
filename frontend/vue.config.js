// Vue CLI configuration, see https://cli.vuejs.org/config
module.exports = {
  // configure the port the development server will run on
  devServer: {
    port: 8000
  },
  // display eslint errors as warnings --> build does not fail on linting issues
  lintOnSave: true
};
